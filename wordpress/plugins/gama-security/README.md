# Gama Security

`gama-software/gama-security` is the first-party least-privilege and request
security boundary. It grants Editors only the additional Site Editor capability,
adds defensive response headers and rate-limits repeated login failures.

It has no paid or external runtime dependency.
