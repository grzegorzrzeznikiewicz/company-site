#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

HOST_UID="${HOST_UID:-$(id -u)}" \
HOST_GID="${HOST_GID:-$(id -g)}" \
"$SCRIPT_DIR/_compose" restart
